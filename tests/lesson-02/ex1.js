const number = 12;
const name = "my number";
let isEven = false;

if (number % 2 === 0) {
    isEven = true;
    //in thêm kết quả với biến name    
    console.log(name + " " + isEven);
};
if (number % 2 === 1) {
    //in thêm kết quả với biến name    
    console.log(name + " " + isEven);
};
